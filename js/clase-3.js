function OrdenHelado(topping = '') {
    var valor_helado = 2000
    var topping = sabor

    if (
        topping == 'oreo'{
        valor_helado = parseInt(valor_helado) + parseInt(800)
        console.log(" El valor total del helado es: $" + valor_helado)
    }
    else
    )
}